# frozen_string_literal: true

RSpec.describe Moralis::Ruby::SDK do
  it "has a version number" do
    expect(Moralis::Ruby::SDK::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
